# Frontend

User-facing surfaces for Uncognito.

- `web` - Next.js Wall of Shame portal and public roast pages.
- `extension` - Chrome Manifest V3 extension for opt-in capture and demo controls.

Frontend code should call `backend/api` for persistence, AI, storage, privacy, and social behavior.
