# Web App

Next.js app for the Wall of Shame portal, roast detail pages, and dynamic Open Graph metadata.

Primary owners:

- Web Vault Agent
- Data Storage Agent

Expected routes:

- `/` - Wall of Shame gallery.
- `/roast/[id]` - Public roast detail page.
- `/admin` - Owner controls, if included.
Backend interactions go through `backend/api`.
