# Backend

Backend API and domain services for Uncognito.

- `api` - HTTP route surface for uploads, roasts, hide/delete, and optional sharing.
- `services` - reusable backend modules for contracts, scheduling, storage, AI, and social integrations.

Provider SDKs and secrets belong here, not in frontend code.
